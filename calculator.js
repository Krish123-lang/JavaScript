let sumData = (a, b) => {
    return a + b;
}
let subData = (a, b) => {
    return a - b;
}
let prodData = (a, b) => {
    return a * b;
}
let divData = (a, b) => {
    return a / b;
}

// export default sumData; //can only export one function
export { sumData, subData, prodData, divData };